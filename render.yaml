services:
  - type: web
    name: test-https-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn test_https:app
    envVars:
      - key: FLASK_ENV
        value: production
